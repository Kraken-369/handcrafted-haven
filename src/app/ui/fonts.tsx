import { Handlee, Jost } from 'next/font/google';
 
export const handlee = Handlee({
  subsets: ['latin'],
  weight: '400'
});

export const jost = Jost({
  subsets: ['latin'],
  weight: ['400', '700', '900']
});
